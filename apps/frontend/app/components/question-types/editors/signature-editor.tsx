"use client";

/** Editor for signature questions — nothing to configure. */
export function SignatureEditor() {
  return (
    <p className="text-sm text-muted-foreground">
      Respondents draw their signature on a pad; it&apos;s saved as an image.
    </p>
  );
}
